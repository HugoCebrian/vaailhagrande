<header class=" text-white items-center w-full md:overflow-hidden z-50 transition-all absolute ">

  <div id="header" class="w-full relative ">

    <div class="flex items-center justify-between border-b border-white/20 h-24 relative z-40 ">
      <div class="flex justify-between flex-wrap md:flex-nowrap items-center h-full w-full   md:w-auto divide-x divide-white/20">
        
        <img class="h-full p-4 " src="/images/logo.png" alt="">
        
        <div id="open"  class="flex flex-col gap-[5px] md:hidden cursor-pointer pr-4">
          <div class="line w-[28px] h-[3px] bg-white rounded-md"></div>
          <div class="line w-[28px] h-[3px] bg-white rounded-md"></div>
          <div class="line w-[28px] h-[3px] bg-white rounded-md"></div>        
        </div>
        
        <nav  class=" md:flex items-center md:w-auto hidden ">
          <ul class="flex p-10 font-raleway font-semibold md:w-auto md:flex gap-4 uppercase text-gray-200">
            <li class="hover:text-red-600 transition-all group"><a href="#home">
              Home
              <div class="max-w-0 lg:group-hover:max-w-12 h-[1px] transition-all duration-700 ease-in-out bg-red-500"></div>
            </a></li>
            <li class="hover:text-orange transition-all group"><a href="#voltailha">
              Volta a Ilha
              <div class="max-w-0 lg:group-hover:max-w-24 h-[1px] transition-all duration-700 ease-in-out bg-red-500"></div>
            </a></li>
            <li class="hover:text-orange transition-all group"><a href="#tragaseuclube">
              Traga seu Clube
              <div class="max-w-0 lg:group-hover:max-w-32 h-[1px] transition-all duration-700 ease-in-out bg-red-500"></div>
            </a></li>
            <li class="hover:text-orange transition-all group"><a href="#social">
              Projeto Social
              <div class="max-w-0 lg:group-hover:max-w-32 h-[1px] transition-all duration-700 ease-in-out bg-red-500"></div>
            </a></li>
            <li class="hover:text-orange transition-all group"><a href="#event">
              Eventos
              <div class="max-w-0 lg:group-hover:max-w-24 h-[1px] transition-all duration-700 ease-in-out bg-red-500"></div>
            </a> </li>
          </ul>
        </nav>
     
      </div>
  
      <div class="hidden md:flex h-full divide-x divide-white/20 text-gray-200 uppercase">
        <a href="" class="flex items-center gap-2 px-6 border-l border-white/20 md:hidden lg:flex">
          <span class="text-2xl"><i class="fa-solid fa-location-dot"></i></span>
          <p class="font-raleway font-bold">Como chegar?</p>
        </a>
        <a href="" class="items-center gap-2 px-6 md:hidden lg:flex">
          <span class="text-2xl"><i class="fa-solid fa-phone"></i></span>
          <p class="font-roboto font-bold">(24) 90000-0000</p>
        </a>
        <a href="" class="h-full text-3xl flex items-center justify-center px-6"><i class="fa-brands fa-instagram"></i></a >
      </div>
    </div>
      
    <div id="menu" class="transition-transform duration-500 transform -translate-x-full w-full h-screen bg-white z-50  top-0 md:hidden fixed">

      <div class="flex justify-end md:hidden p-8">
        <div id="close" class="relative flex items-center justify-center w-[28px] h-[28px] cursor-pointer">
          <div class="absolute w-[28px] h-[3px] bg-offblack rounded-md transform rotate-45"></div>

          <div class="absolute w-[28px] h-[3px] bg-offblack rounded-md transform -rotate-45"></div>
        </div>
      </div>
    
      <nav class="px-8">
        <ul class="text-black font-futura uppercase text-2xl flex flex-col gap-4">
          <li><a href="#">Portfolio</a></li>
          <li><a href="#">Processos</a></li>
          <li><a href="#">Sobre</a></li>
        </ul>
      </nav>
    
      <div class="p-8">
        <a class="px-8 py-3 border-2 border-orange text-orange font-futura" href="#">Fazer uma reserva</a>
      </div>
    </div>

  </div>
</header>

<script>

  document.addEventListener("DOMContentLoaded", () => {
    const header = document.getElementById("header") as HTMLElement | null;
    let lastScrollTop = 0;

    // Função para aplicar as transições
    const applyTransition = (styles: Partial<CSSStyleDeclaration>) => {
      if (header) {
        Object.assign(header.style, styles);
      }
    };

  // Função para tratar o scroll 
  const handleScroll = () => {
  const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

    if (scrollTop > lastScrollTop) {
      if (header && scrollTop > header.offsetHeight) {
        applyTransition({
          transition: "transform 0.6s ease-out, opacity 0.6s ease-out, background-color 0.6s ease-out",
          transform: "translateY(-100%)",
          opacity: "0",
          backgroundColor: "transparent"
        });
      }
    } else {
      if (scrollTop === 0 && header) {
        applyTransition({
          transition: "transform 0.2s ease-in-out, opacity 0.3s ease-in-out, background-color 0.3s ease-in-out",
          transform: "translateY(0%)",
          opacity: "1",
          position: "",
          backgroundColor: "transparent",
        });
      } else if (header) {
        applyTransition({
          transition: "transform 0.4s ease-in-out, opacity 0.3s ease-in-out, background-color 0.3s ease-in-out",
          transform: "translateY(0%)",
          opacity: "1",
          position: "fixed",
          backgroundColor: "rgba(17, 24, 39)",
        });
      }
    }

    lastScrollTop = scrollTop;
    };

    // Adiciona um ouvinte de evento de rolagem (scroll) ao documento
    document.addEventListener("scroll", handleScroll);
    });



    document.getElementById('open')?.addEventListener('click', () => {
      document.getElementById('menu')?.classList.remove('-translate-x-full');
      setTimeout(() => {
          document.body.classList.add('overflow-hidden'); // Adiciona overflow-hidden ao body após 3 segundos
          console.log('Menu aberto, overflow-hidden adicionado ao body após 3 segundos');
      }, 200); // 3000 milissegundos = 3 segundos
    });

    document.getElementById('close')?.addEventListener('click', () => {
        document.getElementById('menu')?.classList.add('-translate-x-full');
        document.body.classList.remove('overflow-hidden'); // Remove overflow-hidden do body imediatamente
        console.log('Menu fechado, overflow-hidden removido do body');
    });
</script>
