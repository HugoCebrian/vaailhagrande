---
interface Props {
  data: string;
	subtitle: string;
	link: string;
	image: string;
}

const {subtitle, link, data, image} = Astro.props;
---



<div class="flex flex-col gap-4"> 
  <div class="grid h-80 bg-cover bg-center" style={{backgroundImage: `url('/images/${image}')`}}>

    <div class="self-start place-self-end m-4 p-2 bg-[#191D31] font-bold">{data}</div>
    <div class="self-end flex">
      <div class="backdrop-blur w-10/12 p-2 py-3 px-4">{subtitle}</div>
      <div class="bg-[#191D31] w-2/12 p-2 py-3 flex items-center justify-center text-2xl">
        <i class="fa-solid fa-chevron-right"></i>
      </div>
    </div>
  </div>

</div>
